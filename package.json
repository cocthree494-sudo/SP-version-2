{
  "name": "support-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Multi-tenant customer-support AI SaaS",
  "packageManager": "npm@11.13.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "dev": "node scripts/dev.mjs",
    "dev:apps": "concurrently --kill-others-on-fail --names api,worker,web,widget --prefix-colors blue,cyan,green,magenta \"npm:dev:api\" \"npm:dev:worker\" \"npm:dev:web\" \"npm:dev:widget\"",
    "dev:api": "uv run --project apps/api uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "dev:worker": "uv run --project apps/api arq app.workers.ingestion.WorkerSettings",
    "dev:web": "npm run dev --workspace @support-agent/web",
    "dev:widget": "npm run dev --workspace @support-agent/widget",
    "infra:up": "docker compose --env-file .env -f infra/compose.yaml up -d --wait",
    "infra:down": "docker compose --env-file .env -f infra/compose.yaml down",
    "db:upgrade": "uv run --project apps/api alembic -c apps/api/alembic.ini upgrade head",
    "db:current": "uv run --project apps/api alembic -c apps/api/alembic.ini current",
    "db:configure-test-role": "uv run --project apps/api python apps/api/configure_test_role.py",
    "build": "npm run build --workspaces --if-present",
    "check": "npm run check:api && npm run check:web && npm run check:widget",
    "check:api": "uv run --project apps/api ruff check apps/api/app apps/api/tests && uv run --project apps/api mypy apps/api/app apps/api/tests && uv run --project apps/api pytest apps/api/tests",
    "check:web": "npm run lint --workspace @support-agent/web && npm run typecheck --workspace @support-agent/web",
    "check:widget": "npm run lint --workspace @support-agent/widget && npm run typecheck --workspace @support-agent/widget",
    "eval:agent": "uv run --project apps/api python apps/api/run_agent_eval.py",
    "e2e:services": "concurrently --kill-others-on-fail --names api,worker,web,fixture --prefix-colors blue,cyan,green,magenta \"npm:dev:api\" \"npm:dev:worker\" \"npm:dev:web\" \"node tests/e2e/fixture-server.mjs\"",
    "test:e2e": "npm run build --workspace @support-agent/widget && playwright test -c tests/e2e/playwright.config.ts",
    "test:e2e:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "concurrently": "^9.2.1"
  }
}
